platform: template
switches:
        sala_tv:
                friendly_name: Sala TV
                value_template: "{{ is_state('binary_sensor.sala_tv_status', 'on') }}"
                turn_off:
                        service: rest_command.assistant_relay
                        data:
                                command: "Spegni Sala TV" 
                turn_on:
                        service: rest_command.assistant_relay
                        data:
                                command: "Accendi Sala TV"
                
