annuncia:
  sequence:
    - condition: state
      entity_id: input_boolean.annunci
      state: 'on'
    - service: var.set
      entity_id: var.alexa_volume
      data_template:
        value: >
          {{ state_attr("media_player.echo_dot_della_sala", "volume_level") }}
    - service: media_player.volume_set 
      entity_id: media_player.echo_dot_della_sala
      data_template:
        volume_level: >
          {% if "Sera" in states.input_select.stato_del_giorno.state %}
            0.5
          {% else %}
            0.75
          {% endif %}
    - delay: 00:00:05
    - service: notify.alexa_media_echo_dot_della_sala
      data_template:
        data:
          type: announce
          method: speak
        message: !include ../../jinja_templates/template.annuncio.yaml
    - delay: 00:00:05
    - service: media_player.volume_set 
      entity_id: media_player.echo_dot_della_sala
      data_template:
        volume_level: >
          {{ states("var.alexa_volume") }}