notifica_famiglia:
  sequence:
    - condition: state
      entity_id: input_boolean.messaggi
      state: 'on'
    - service: notify.family
      data_template:
        message: "{{ message }}"