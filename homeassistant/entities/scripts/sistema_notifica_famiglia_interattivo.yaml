sistema_notifica_famiglia_interattivo:
  sequence:
    - condition: or
      conditions:
      - condition: state
        entity_id: input_boolean.messaggi
        state: 'on'
      - condition: template
        value_template: '{{ urgente }}'
    - service: notify.family
      data_template:
        message: "{{ message }}"
        data:
          inline_keyboard: "{{ inline_keyboard }}"
