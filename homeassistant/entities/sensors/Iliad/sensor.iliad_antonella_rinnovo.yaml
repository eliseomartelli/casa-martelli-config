platform: template
sensors:
  iliad_antonella_rinnovo:
    value_template: >
      {{ ((as_timestamp(state_attr("sensor.iliad_antonella", "next_renewal")) - as_timestamp(now())) / (3600*24)) | int }}
