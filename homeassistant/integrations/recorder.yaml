recorder:
  purge_keep_days: 5
  include:
    entities:
      - binary_sensor.presenza_cortile_viale
      - binary_sensor.presenza_cortile_piazzale

      - binary_sensor.soffitta_lavatrice_hoover

      - sensor.modem_banda_download
      - sensor.modem_banda_upload