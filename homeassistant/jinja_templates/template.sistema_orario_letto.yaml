>
  {%- set tstamp = state_attr('input_datetime.sistema_inizio_notte_invernale', 'timestamp') -%}
  {%- if states('sensor.season') in ['summer', 'spring'] -%}
    {%- set tstamp = state_attr('input_datetime.sistema_inizio_notte_estivo', 'timestamp') -%}
  {%- endif -%}
  {%- set delta = 9000 if states('sensor.season') in ['summer', 'spring'] else 7200 -%}
  {{ ((tstamp | int - delta) | timestamp_custom('%H:%M', False)) == states('sensor.time') }}
